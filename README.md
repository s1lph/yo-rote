# yo.route - Логистическая платформа для малого бизнеса. Степень готовности около 80%

Веб-приложение для оптимизации маршрутов доставки с автоматическим геокодингом и визуализацией на интерактивной карте.

##  Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Получите API ключ OpenRouteService

> **Важно:** Без API ключа геокодинг и оптимизация маршрутов работать не будут!

1. Зарегистрируйтесь на https://openrouteservice.org/dev/#/signup
2. Создайте токен в разделе "Tokens"
3. Скопируйте ключ

### 3. Настройте `.env` файл

```env
ORS_API_KEY=ваш_ключ_здесь
DATABASE_URL=sqlite:///logistics.db
FLASK_ENV=development
```

### 4. Запустите приложение

```bash
python app.py
```

Откройте http://localhost:5000

## Реализованный функционал

### Backend
-  **База данных SQLite** с моделями: User, Courier, Order, Route
-  **Автоматический геокодинг** адресов через OpenRouteService
-  **Оптимизация маршрутов (VRP)** с учетом типа транспорта
-  **REST API** для всех операций

### Frontend
-  **Интерактивная карта Leaflet.js** с визуализацией маршрутов
-  Маркеры заказов с номерами (порядок посещения)
-  Отображение депо/склада
-  Линия оптимального маршрута

## 📖 Основные API Endpoints

### Заказы
- `GET /api/orders` - список заказов (фильтры: date, status, search)
- `POST /api/orders` - создание заказа с **автоматическим геокодингом**
- `PUT /api/orders/<id>` - обновление заказа
- `DELETE /api/orders/<id>` - удаление заказа

### Маршруты
- `GET /api/routes` - список маршрутов (фильтры: date, courier_id, status)
- **`POST /api/routes/optimize`** - оптимизация маршрута через VRP алгоритм
- **`GET /api/routes/<id>/optimize-view`** - данные для визуализации на карте
- `DELETE /api/routes/<id>` - удаление маршрута

### Курьеры
- `GET /api/couriers` - список курьеров
- `POST /api/couriers` - добавление курьера
- `PUT /api/couriers/<id>` - обновление
- `DELETE /api/couriers/<id>` - удаление


## 🛠️ Технологии

- **Backend**: Flask, SQLAlchemy, OpenRouteService
- **Frontend**: Leaflet.js, Vanilla JavaScript
- **База данных**: SQLite 
- **API**: OpenRouteService (геокодинг + VRP оптимизация)



## 🚧 Следующие шаги

- [ ] Аутентификация пользователей (JWT)
- [ ] Telegram Bot для уведомлений курьеров
- [ ] Временные окна доставки
- [ ] Множественные депо
- [ ] Статистика и аналитика

---

**Лицензия**: MIT  
**Автор**: yo.route team
