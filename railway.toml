[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2"
releaseCommand = "python quick_migrate.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
